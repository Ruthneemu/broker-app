{
  "scripts": {
    "build": "npm run build:frontend && npm run build:admin && npm run build:backend",
    "build:frontend": "cd frontend && chmod +x node_modules/.bin/react-scripts && npm run build",
    "build:admin": "cd admin && npm install && chmod +x node_modules/.bin/react-scripts && npm run build"  }
}
